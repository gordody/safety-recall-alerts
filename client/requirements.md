## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already in repo.
No additional dependencies required.
All API calls use @shared/routes schemas for validation; cookies via credentials: "include".
